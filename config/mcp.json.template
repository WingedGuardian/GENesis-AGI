{
  "mcpServers": {
    "codebase-memory-mcp": {
      "command": "{{GENESIS_ROOT}}/.claude/mcp/run-codebase-memory",
      "args": []
    },
    "genesis-health": {
      "command": "{{GENESIS_ROOT}}/.claude/mcp/run-mcp-server",
      "args": ["--server", "health"]
    },
    "genesis-memory": {
      "command": "{{GENESIS_ROOT}}/.claude/mcp/run-mcp-server",
      "args": ["--server", "memory"]
    },
    "genesis-outreach": {
      "command": "{{GENESIS_ROOT}}/.claude/mcp/run-mcp-server",
      "args": ["--server", "outreach"]
    },
    "genesis-recon": {
      "command": "{{GENESIS_ROOT}}/.claude/mcp/run-mcp-server",
      "args": ["--server", "recon"]
    }
  }
}
