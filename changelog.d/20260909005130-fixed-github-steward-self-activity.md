- **GitHub steward: stop reporting your own activity as someone else's.** A mention notification's actor was resolved as "whoever commented on the thread most recently", falling back to the thread's author when no comment was available. On your own pull requests both answers are *you*, so routine review-bot traffic arrived as a Telegram digest entry with no contributor attached. The actor for a mention is now the author of the comment that actually names you — which is usually a review bot, filtered as automation like any other. Two real gaps close with it: mentions written in a pull request's inline review comments are now seen at all, and a response on a pull request you opened on someone else's repo is no longer discarded as self-activity.
