- **Build sessions now verify before they open a PR, when this install can show the result.** The genesis-development skill adds a build step: scripts, hooks and CLIs are run from the branch against real input before the PR opens, risky surfaces ship behind a shadow flag, and when acceptance needs the owner's hands the session iterates with them and opens the PR once they confirm. Runtime code keeps its post-merge `E2E:` obligation until a supported way to run a branch in the live runtime exists.
