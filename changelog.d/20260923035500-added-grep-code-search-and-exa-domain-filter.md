- Every install now gets the Grep code-search MCP server (`searchGitHub`) registered automatically — literal/regex search across roughly a million public GitHub repositories, with no API key to obtain. `web_search` also gained `include_domains` / `exclude_domains`, so the documented recipe for scoping an Exa search to a single site now actually takes effect instead of silently searching the whole web.
