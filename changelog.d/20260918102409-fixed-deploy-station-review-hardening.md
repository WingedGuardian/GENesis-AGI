- Hardened the deploy station after review: failed code-only deploys past the tree-advance point now raise a critical alert instead of only a ledger row; validation wrappers forward termination to the wrapped command (bounded, then SIGKILL) so a cancelled run cannot keep testing with the lock released; `validated`/`deployed` receipts now verify the checkout's identity and cleanliness before recording; the guardian lease renewer no longer inherits update.sh's lock fd; worktree refusal detects linked worktrees via Git rather than path substrings; receipt pruning propagates lock setup faults instead of reporting them as a busy station; operator-stopped updates record `deployed_not_started` rather than claiming a serving SHA.
