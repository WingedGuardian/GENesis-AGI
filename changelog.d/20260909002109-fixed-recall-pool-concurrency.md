- **Memory recall no longer times out when several sessions are active at once.** Recall reads share a small pool of database connections, and once every connection was in use the next recall waited — long enough to exceed its time budget and fall back to a keyword-only search. The pool is now sized from the machine's CPU count instead of a fixed four, so concurrent sessions no longer queue behind each other.
