- **Updated the routed model versions.** The routing table had drifted behind the providers, so several call sites were asking for models that had since been superseded. Providers now point at current models. Two things this change deliberately does NOT touch: the NVIDIA NIM DeepSeek provider, whose model reached end of life and now answers HTTP 410 (18 call sites reference it, and NIM serves no DeepSeek replacement, so repointing them is its own decision), and the eight providers that are defined but wired to no call site.
