- **Code intelligence indexing no longer gets killed partway through.** The memory cap on the indexing job was set below what indexing this codebase actually needs, so the job was repeatedly killed and the index never completed. The cap is now set from a measurement of the real peak, and the job is marked as the first thing the system should sacrifice if memory ever does run short — so a shortage costs an index rather than your session or the server.
