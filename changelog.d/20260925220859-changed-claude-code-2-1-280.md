- Genesis now pins Claude Code **2.1.280** (was 2.1.246); `scripts/update.sh` aligns both the container and the host. This range fixes a startup stall and a headless hang on the recovery path, and makes the `opus` model alias resolve to Opus 5.5. `install.sh` also turns off syncing your claude.ai skills and plugins into Claude Code on that machine (`syncClaudeAiSkills` / `syncClaudeAiPlugins` set to `false`, only where you have not set them yourself — set either to `true` to keep the sync). If that machine had already synced claude.ai skills, Claude Code moves them to `~/.claude/skills/.trash` at its next launch; re-enabling downloads them again.
